<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_partner_form_mimsms_history" model="ir.ui.view">
        <field name="name">res.partner.form.mimsms.history</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <xpath expr="//div[@name='button_box']" position="inside">
                <button name="action_view_sms_history"
                        type="object"
                        class="oe_stat_button"
                        icon="fa-comments"
                        groups="mimsms_gateway.group_sms_gateway_user">
                    <field name="sms_history_count"
                           widget="statinfo"
                           string="SMS History"/>
                </button>
            </xpath>
        </field>
    </record>

    <record id="action_contacts_send_mimsms" model="ir.actions.act_window">
        <field name="name">Send SMS</field>
        <field name="res_model">mimsms.composer</field>
        <field name="view_mode">form</field>
        <field name="view_id" ref="mimsms_gateway.view_sms_composer_form"/>
        <field name="target">new</field>
        <field name="binding_model_id" ref="base.model_res_partner"/>
        <field name="binding_view_types">list</field>
        <field name="group_ids" eval="[(4, ref('mimsms_gateway.group_sms_gateway_user'))]"/>
        <field name="context">{'default_composition_mode1': 'bulk'}</field>
    </record>
</odoo>
